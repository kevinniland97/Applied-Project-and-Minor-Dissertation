import React from 'react';
import '../styling/UserPages.css';

const UserPages = props => (
    <nav className="userPages">
        <ul>
            <li><a href="/">Sign in</a></li>
        </ul>
    </nav>
);

export default UserPages;